# Reviewer instructions

Review blocks `R01` through `R12` without access to the sealed A/B identity key.

For each block answer exactly one of:

- `A>B`
- `B>A`
- `equivalent`
- `unreviewable`

Use one criterion only:

> Which side contains the stronger portfolio of mathematical forms worth keeping or developing further?

Each side contains three forms, each shown at `t=30`, `t=90`, and `t=150`. Judge the side as a portfolio rather than selecting a single favorite frame.

Do not infer policy identity from ordering or visual style. The A/B assignment is sealed until all 12 judgments are fixed.
