# Family projected-spectral artistic review v1

## Question

Does the mechanically validated family-specific projected-spectral runtime produce mathematical forms that an independent human reviewer prefers to the exact native-only runtime at the same 20-challenger budget?

This was the missing artistic-authority layer after:

1. family-law projected spectral primitive (#131);
2. equal-budget portfolio confirmation (#133);
3. fresh integrated runtime replay (#134).

No structural target, semantic scorer, model judge, or post-hoc oracle chose the displayed forms.

## Frozen comparison

Route: `family` only.

For every fresh review seed:

- both arms began from the exact same hard-valid start genome and phenotype;
- baseline used the runtime default `native-only`;
- candidate used `native-family-projected-spectral-50-50-v1`;
- both arms used the same `DeterministicTemporalSelector` only as the runtime trajectory driver;
- both arms generated exactly 20 challengers;
- baseline spent 20 native attempts;
- candidate spent exactly 10 native + 10 family projected-spectral attempts;
- the displayed form was each arm's own runtime `provisionalChampion`;
- only raw frames at `t=30,90,150` were shown.

The projected-spectral operator remained the frozen K=2 / amplitude-16 Hamiltonian warp with the #131 family projection that preserves each sibling's native anchor→terminal length around the warped anchor.

## Fresh review population

Excluded smoke seed: `765999`.

Authoritative review seeds:

`765003, 765019, 765037, 765053, 765071, 765089, 765107, 765127, 765149, 765167, 765181, 765199, 765223, 765239, 765257, 765277, 765293, 765311, 765331, 765349, 765367, 765383, 765401, 765419`.

Exactly 24 authoritative blocks were generated. No seed was replaced, resampled, or tuned after review began.

## Blind custody

Reviewer-facing blocks exposed only block ID, side A, side B, and raw temporal frames.

Authoritative A/B orientation was randomized from a cryptographically random salt generated inside GitHub Actions. The salt and full identity mapping were written only to a separately uploaded key artifact.

Frozen generation:

- workflow run `33757449648`;
- generation head `925088143c4dd23eebbd8f8c09ace49544dc757b`;
- reviewer artifact `9894173721`, digest `sha256:55ffd3093af6c29f4875f71cdc86ac706b268767b871273569de5414d0ca9e1b`;
- sealed key artifact `9894174297`, digest `sha256:920472aca27e8ac38fe5ec018bfd57bdfc3c3268bd0ce97b46d6fae8e39a9ef7`.

Human ratings were fixed first at commit `bf04087d976d64eea40a991f40456032cdc4b10a`. Only then was the identity-key archive downloaded/opened; the downloaded archive SHA-256 matched the frozen key artifact digest exactly.

## Human question

> Which candidate is the stronger mathematical form worth keeping or developing further?

Allowed judgments were `A>B`, `B>A`, `equivalent`, `unreviewable`.

## Preregistered support gate

`FAMILY_PROJECTED_SPECTRAL_ARTISTIC_SUPPORT` required all:

1. at least 18/24 blocks reviewable;
2. at least 12 decisive blocks;
3. projected-spectral decisive win rate strictly greater than 0.65;
4. one-sided exact binomial p-value <= 0.10 against p=0.5 on decisive blocks.

## Terminal result

**`FAMILY_PROJECTED_SPECTRAL_ARTISTIC_SUPPORT_NOT_DEMONSTRATED`**.

Observed:

- reviewable: `24/24` — pass;
- decisive: `6/24` — fail;
- projected wins: `2/6` (`0.3333`) — fail;
- native wins: `4/6`;
- equivalent: `18/24`;
- one-sided exact binomial p: `0.890625` — fail.

The strong mechanical advantage from #131/#133/#134 did not translate into artistic preference. The family projected-spectral mode remains mechanically supported, opt-in, and default-off. This exact artistic-promotion line is closed: do not tune amplitude, bandwidth, projection, schedule, selector behavior, or seeds against consumed `765xxx` ratings.

Per the frozen boundary, this terminal result releases the parked recurrent learned-operator branch as the next independent research direction.
