# Family projected-spectral artistic review v1

## Question

Does the mechanically validated family-specific projected-spectral runtime produce mathematical forms that an independent human reviewer prefers to the exact native-only runtime at the same 20-challenger budget?

This is the missing artistic-authority layer after:

1. family-law projected spectral primitive (#131);
2. equal-budget portfolio confirmation (#133);
3. fresh integrated runtime replay (#134).

No structural target, semantic scorer, model judge, or post-hoc oracle chooses the displayed forms.

## Frozen comparison

Route: `family` only.

For every fresh review seed:

- both arms begin from the exact same hard-valid start genome and phenotype;
- baseline uses the runtime default `native-only`;
- candidate uses `native-family-projected-spectral-50-50-v1`;
- both arms use the same `DeterministicTemporalSelector` only as the runtime trajectory driver;
- both arms generate exactly 20 challengers;
- baseline spends 20 native attempts;
- candidate spends exactly 10 native + 10 family projected-spectral attempts;
- the displayed form is each arm's own runtime `provisionalChampion`;
- only raw frames at `t=30,90,150` are shown.

The projected-spectral operator remains the frozen K=2 / amplitude-16 Hamiltonian warp with the #131 family projection that preserves each sibling's native anchor→terminal length around the warped anchor.

## Fresh review population

Excluded smoke seed:

`765999`

Authoritative review seeds, one blinded block per seed:

`765003, 765019, 765037, 765053, 765071, 765089, 765107, 765127, 765149, 765167, 765181, 765199, 765223, 765239, 765257, 765277, 765293, 765311, 765331, 765349, 765367, 765383, 765401, 765419`

Exactly 24 authoritative blocks are generated. No seed may be replaced, resampled, or tuned after review begins.

## Blind custody

Reviewer-facing blocks expose only:

- block ID;
- side A;
- side B;
- raw temporal frames.

They do not expose seed, runtime mode, candidate ID, genome, operator history, selection status, or A/B identity.

For the authoritative package, A/B orientation is randomized from a cryptographically random salt generated inside the GitHub Actions review job. That salt and the full identity mapping are written only to a separately uploaded key artifact. The salt is never printed to logs or included in the reviewer artifact.

The first successful authoritative generation run is frozen by workflow run ID, head SHA, reviewer artifact digest, and key artifact digest before human ratings are accepted. The key must remain unopened until all 24 ratings are fixed in Git.

## Human question

> Which candidate is the stronger mathematical form worth keeping or developing further?

Allowed judgments:

- `A>B`
- `B>A`
- `equivalent`
- `unreviewable`

`equivalent` is reviewable but non-decisive. `unreviewable` is neither reviewable nor decisive.

## Preregistered support gate

`FAMILY_PROJECTED_SPECTRAL_ARTISTIC_SUPPORT` requires all:

1. at least 18/24 blocks reviewable;
2. at least 12 decisive blocks;
3. projected-spectral decisive win rate strictly greater than 0.65;
4. one-sided exact binomial p-value <= 0.10 against p=0.5 on decisive blocks.

No morphology, target-recovery, runtime-selector, or mechanical score enters this decision.

## Consequence

Positive evidence establishes artistic usefulness for this reviewer and this frozen family runtime comparison. It closes the family projected-spectral causal chain successfully and permits later consideration of broader/default family integration, but does not automatically change the default.

Negative evidence leaves the operator mechanically supported and opt-in, closes artistic promotion of this exact family runtime treatment, and forbids nearby seed/schedule/amplitude/projection tuning on the consumed `765xxx` review population.

Either terminal result releases the parked recurrent learned-operator branch as the next independent research direction.
