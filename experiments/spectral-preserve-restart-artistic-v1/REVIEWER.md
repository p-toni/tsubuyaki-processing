# Reviewer instructions

Review only the reviewer-facing images and `review-contract.json`. Do not inspect repository code, workflow logs, artifact metadata beyond the reviewer package, or the separately uploaded A/B key before all judgments are fixed.

For each R01-R12 answer exactly one of:

- `A>B`
- `B>A`
- `equivalent`
- `unreviewable`

Question:

> **Which side contains the stronger portfolio of mathematical forms worth keeping or developing further?**

Judge the three-form portfolio as delivered, including any redundancy. Do not reward or penalize a side because you think you recognize the treatment. If the sides are visually equivalent, use `equivalent`.
