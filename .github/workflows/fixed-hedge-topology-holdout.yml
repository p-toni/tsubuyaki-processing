name: fixed-hedge-topology-holdout

on:
  pull_request:
    paths:
      - "experiments/fixed-hedge-topology-v1/**"
      - ".github/workflows/fixed-hedge-topology-holdout.yml"

concurrency:
  group: fixed-hedge-topology-holdout-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  block:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        route: [recurrence, orbit, family, sheet, filament]
        seed: [179, 181, 191]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install prototype dependencies
        run: python -m pip install -r prototypes/autonomous-discovery/requirements.txt
      - name: Run untouched fixed hedge block
        shell: bash
        run: |
          set -o pipefail
          python experiments/fixed-hedge-topology-v1/policy.py \
            --route "${{ matrix.route }}" \
            --seed "${{ matrix.seed }}" \
            --shares "0.5" \
            | tee "/tmp/fixed-hedge-holdout-${{ matrix.route }}-${{ matrix.seed }}.json"
      - name: Upload holdout block
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fixed-hedge-holdout-${{ matrix.route }}-${{ matrix.seed }}
          path: /tmp/fixed-hedge-holdout-${{ matrix.route }}-${{ matrix.seed }}.json
          if-no-files-found: warn

  aggregate-holdout:
    needs: block
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install prototype dependencies
        run: python -m pip install -r prototypes/autonomous-discovery/requirements.txt
      - uses: actions/download-artifact@v4
        with:
          pattern: fixed-hedge-holdout-*
          path: /tmp/fixed-hedge-holdout-results
          merge-multiple: true
      - name: Apply frozen strict fixed hedge holdout gate
        shell: bash
        run: |
          set -o pipefail
          python experiments/fixed-hedge-topology-v1/aggregate_holdout.py \
            --results-dir /tmp/fixed-hedge-holdout-results \
            | tee /tmp/fixed-hedge-holdout-summary.json
      - name: Upload holdout summary
        uses: actions/upload-artifact@v4
        with:
          name: fixed-hedge-holdout-summary
          path: /tmp/fixed-hedge-holdout-summary.json
