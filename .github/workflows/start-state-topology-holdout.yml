name: start-state-topology-holdout

on:
  pull_request:
    paths:
      - "experiments/start-state-topology-v1/**"
      - ".github/workflows/start-state-topology-holdout.yml"

concurrency:
  group: start-state-topology-holdout-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  block:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        route: [recurrence, orbit, family, sheet, filament]
        seed: [149, 151, 157]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install prototype dependencies
        run: python -m pip install -r prototypes/autonomous-discovery/requirements.txt
      - name: Run untouched start-state topology block
        shell: bash
        run: |
          set -o pipefail
          python experiments/start-state-topology-v1/selector.py \
            --route "${{ matrix.route }}" \
            --seed "${{ matrix.seed }}" \
            --thresholds "0.5" \
            | tee "/tmp/start-state-holdout-${{ matrix.route }}-${{ matrix.seed }}.json"
      - name: Upload holdout block
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: start-state-holdout-${{ matrix.route }}-${{ matrix.seed }}
          path: /tmp/start-state-holdout-${{ matrix.route }}-${{ matrix.seed }}.json
          if-no-files-found: warn

  aggregate-holdout:
    needs: block
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install prototype dependencies
        run: python -m pip install -r prototypes/autonomous-discovery/requirements.txt
      - uses: actions/download-artifact@v4
        with:
          pattern: start-state-holdout-*
          path: /tmp/start-state-holdout-results
          merge-multiple: true
      - name: Apply frozen strict holdout gate
        shell: bash
        run: |
          set -o pipefail
          python experiments/start-state-topology-v1/aggregate_holdout.py \
            --results-dir /tmp/start-state-holdout-results \
            | tee /tmp/start-state-holdout-summary.json
      - name: Upload holdout summary
        uses: actions/upload-artifact@v4
        with:
          name: start-state-holdout-summary
          path: /tmp/start-state-holdout-summary.json
