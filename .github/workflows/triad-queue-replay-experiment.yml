name: triad-queue-replay-experiment

on:
  pull_request:
    paths:
      - "experiments/triad-queue-replay-v1/**"
      - "prototypes/autonomous-discovery/triad_pair_matrix_review_queue.py"
      - ".github/workflows/triad-queue-replay-experiment.yml"

permissions:
  contents: read

jobs:
  replay:
    strategy:
      fail-fast: false
      matrix:
        seed: [7, 19, 43]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install prototype dependencies
        run: python -m pip install -r prototypes/autonomous-discovery/requirements.txt
      - name: Run file-backed queue replay
        run: |
          python experiments/triad-queue-replay-v1/reproduce.py \
            --seed ${{ matrix.seed }} \
            | tee /tmp/triad-queue-replay-${{ matrix.seed }}.json
      - name: Upload seed result
        uses: actions/upload-artifact@v4
        with:
          name: triad-queue-replay-seed-${{ matrix.seed }}
          path: /tmp/triad-queue-replay-${{ matrix.seed }}.json
