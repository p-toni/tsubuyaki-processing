name: complete-workflow-audit-v1

on:
  pull_request:
    paths:
      - 'experiments/complete-workflow-audit-v1/**'
      - '.github/workflows/complete-workflow-audit-v1.yml'
  workflow_dispatch:

permissions:
  contents: read

env:
  BASE: b8cb5d26853f380f70746bdb9613512d47d55056

jobs:
  simple-arm-final-revisions:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Enforce frozen prior reviews and arm ordering
        run: |
          set -euo pipefail
          test "$(git merge-base "$BASE" HEAD)" = "$BASE"
          test -f experiments/complete-workflow-audit-v1/README.md
          test -f experiments/complete-workflow-audit-v1/briefs.json
          test -f experiments/complete-workflow-audit-v1/simple-arm-candidates-v1.json
          test -f experiments/complete-workflow-audit-v1/simple-arm-readable-v1.md
          test -f experiments/complete-workflow-audit-v1/simple-arm-review-v1.json
          test -f experiments/complete-workflow-audit-v1/simple-arm-candidates-v2-revisions.json
          test -f experiments/complete-workflow-audit-v1/simple-arm-review-v2.json
          test -f experiments/complete-workflow-audit-v1/simple-arm-candidates-v3-revisions.json
          python - <<'PY'
          import json
          a=json.load(open('experiments/complete-workflow-audit-v1/simple-arm-review-v1.json'))
          b=json.load(open('experiments/complete-workflow-audit-v1/simple-arm-review-v2.json'))
          assert a['sourceArtifact']['digest'] == 'sha256:845a19c3b3941ba325cbe7d756d5030d95e42018b514bd244cd3717bf03adc7d'
          assert b['sourceArtifact']['digest'] == 'sha256:70d7714b002ab84021dbcd6da38e48a4f95c8a17843731b93cdbb050c45641a5'
          assert a['researchArmGeneratedOrInspectedBeforeDecision'] is False
          assert b['researchArmGeneratedOrInspectedBeforeDecision'] is False
          assert sorted(k for k,v in b['decisions'].items() if v['decision']=='revise') == ['B01','B02']
          assert sorted(k for k,v in b['decisions'].items() if v['decision']=='stop') == ['B05','B06']
          PY
          if test -e experiments/complete-workflow-audit-v1/research-arm; then
            echo 'research arm cannot exist before the simple arm is frozen' >&2
            exit 1
          fi
          python -m py_compile experiments/complete-workflow-audit-v1/verify_simple_candidates.py
          node --check experiments/complete-workflow-audit-v1/render_exact_post.mjs
      - name: Install image dependency
        run: python -m pip install Pillow
      - name: Verify third-pass B01/B02 revisions
        run: |
          python experiments/complete-workflow-audit-v1/verify_simple_candidates.py \
            --candidates experiments/complete-workflow-audit-v1/simple-arm-candidates-v3-revisions.json \
            --out simple-arm-final-revisions > simple-arm-final-revisions.log
          cat simple-arm-final-revisions/verification.json
      - uses: actions/upload-artifact@v4
        with:
          name: complete-workflow-audit-v1-simple-final-revisions
          path: |
            simple-arm-final-revisions/
            simple-arm-final-revisions.log
