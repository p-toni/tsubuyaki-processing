name: start-state-topology-calibration

on:
  pull_request:
    paths:
      - "experiments/start-state-topology-v1/**"
      - ".github/workflows/start-state-topology-calibration.yml"

concurrency:
  group: start-state-topology-calibration-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  block:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        route: [recurrence, orbit, family, sheet, filament]
        seed: [101, 103, 107, 109, 113, 127, 131, 137, 139]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install prototype dependencies
        run: python -m pip install -r prototypes/autonomous-discovery/requirements.txt
      - name: Run start-state threshold calibration block
        shell: bash
        run: |
          set -o pipefail
          python experiments/start-state-topology-v1/selector.py \
            --route "${{ matrix.route }}" \
            --seed "${{ matrix.seed }}" \
            | tee "/tmp/start-state-${{ matrix.route }}-${{ matrix.seed }}.json"
      - name: Upload calibration block
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: start-state-${{ matrix.route }}-${{ matrix.seed }}
          path: /tmp/start-state-${{ matrix.route }}-${{ matrix.seed }}.json
          if-no-files-found: warn

  aggregate-calibration:
    needs: block
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install prototype dependencies
        run: python -m pip install -r prototypes/autonomous-discovery/requirements.txt
      - uses: actions/download-artifact@v4
        with:
          pattern: start-state-*
          path: /tmp/start-state-results
          merge-multiple: true
      - name: Apply frozen threshold calibration rule
        shell: bash
        run: |
          set -o pipefail
          python experiments/start-state-topology-v1/aggregate_calibration.py \
            --results-dir /tmp/start-state-results \
            | tee /tmp/start-state-calibration-summary.json
      - name: Upload calibration summary
        uses: actions/upload-artifact@v4
        with:
          name: start-state-calibration-summary
          path: /tmp/start-state-calibration-summary.json
